# Ruby deps cache (installed in CI; Gemfile + Gemfile.lock are committed)
vendor/bundle/
.bundle/

# CI scratch files written by the validation workflow
changed.json
comment.md

# Secrets — Cloudflare token, zone/account IDs live in GitHub Secrets / Worker env
.dev.vars
.env
.env.*

# OS noise
.DS_Store
